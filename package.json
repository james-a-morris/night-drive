{
  "name": "night-drive",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 5173",
    "build": "rm -rf dist && mkdir -p dist/src && cp index.html dist/ && cp src/main.js src/style.css dist/src/"
  }
}
